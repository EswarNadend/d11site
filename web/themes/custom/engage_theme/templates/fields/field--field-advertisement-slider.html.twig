{# {{ attach_library('engage_theme/swiper') }} #}
<div class="swiper mySwiper w-full overflow-hidden mt-6">
    <div class="swiper-wrapper">
        {% for item in items %}
        <div class="swiper-slide">    
                {{ item.content }}
        </div>
        {% endfor %}
    </div>
    <div class="swiper-pagination"></div>
</div>

