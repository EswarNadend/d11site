<div class="text-center py-10">
  {% if status == 1 and formData == 'change-password' %}
    <h2 class="text-2xl font-semibold text-green-600">✅ Password Changed Successfully</h2>
    <p class="mt-4">{{ message ?: 'You can now use your new password to log in.' }}</p>
  
  {% elseif not status == 0 and formData == 'change-password' %}
    <h2 class="text-2xl font-semibold text-red-600">❌ Password Change Failed</h2>
    <p class="mt-4">{{ message ?: 'There was a problem changing your password. Please try again.' }}</p>
  {% endif %}
</div>
